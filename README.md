# JavaScript Interview Questions (বাংলায় সহজ ব্যাখ্যা)

## 1️⃣ What is the difference between null and undefined?

### 🔹 undefined:

-   যখন কোনো ভ্যারিয়েবল declare করা হয় কিন্তু value assign করা হয় না,
    তখন সেটি `undefined` থাকে।
-   এটি JavaScript নিজে থেকে সেট করে।

### 🔹 null:

-   `null` মানে ইচ্ছাকৃতভাবে কোনো value নেই।
-   এটি programmer নিজে assign করে।

------------------------------------------------------------------------

## 2️⃣ What is the use of the map() function in JavaScript? How is it different from forEach()?

### 🔹 map() কী করে?

-   একটি array এর প্রতিটি উপাদানের উপর কাজ করে।
-   নতুন একটি array return করে।

### 🔹 forEach() কী করে?

-   প্রতিটি উপাদানের উপর কাজ করে।
-   কিন্তু কোনো নতুন array return করে না।

------------------------------------------------------------------------

## 3️⃣ What is the difference between == and ===?

### 🔹 == (Loose Equality)

-   শুধু value তুলনা করে।
-   প্রয়োজন হলে type convert করে।

### 🔹 === (Strict Equality)

-   value এবং type দুটোই তুলনা করে।
-   type convert করে না।

------------------------------------------------------------------------

## 4️⃣ What is the significance of async/await in fetching API data?

### 🔹 সমস্যা:

API call করতে সময় লাগে (asynchronous operation)।

### 🔹 async/await কী করে?

-   asynchronous কোডকে synchronous এর মতো readable করে।
-   Promise handle করা সহজ হয়।

### সুবিধা:

-   কোড পরিষ্কার ও সহজবোধ্য হয়
-   Error handling সহজ হয়
-   Callback hell এড়ানো যায়

------------------------------------------------------------------------

## 5️⃣ Explain the concept of Scope in JavaScript (Global, Function, Block).

Scope মানে হলো --- কোন ভ্যারিয়েবল কোথায় accessible।

### 🔹 Global Scope:

-   পুরো ফাইলে ব্যবহার করা যায়।

### 🔹 Function Scope:

-   ফাংশনের ভিতরে declare করলে বাইরে থেকে access করা যায় না।

### 🔹 Block Scope:

-   `{}` এর ভিতরে declare করা (let, const)।

------------------------------------------------------------------------

প্রস্তুতকারক: Hosen Ahmed
